.text-center.text-white
  %h1 Hello on MustSee!
  %p You must see whats on it
  - if user_signed_in?
    = link_to 'Browse now!', films_path, class:'btn btn-danger'
  - if current_user.try(:admin?)
    = link_to 'Add new movie', new_film_path, class:'btn btn-danger'
